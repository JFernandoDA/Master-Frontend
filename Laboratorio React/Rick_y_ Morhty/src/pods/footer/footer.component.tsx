import React, { Children } from "react";
import { footerStyle } from "../../layout/pageStyles"


export const Footer: React.FC = () => {
  return (
    <div className={footerStyle}>
    </div>
  );
};